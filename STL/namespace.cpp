#include <bits/stdc++.h>
using namespace std;// std is the default name of namespace scope in cpp which stores all the reiqired function for input and outpus and many more
namespace souvick{
    int k=10;
}
//namespace is just like a scope which can contain variable as well as function which can be used using double colon along with its name
int main()
{
    cout<<souvick::k<<endl;
 return 0;
}
